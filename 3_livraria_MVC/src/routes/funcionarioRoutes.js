// Dependencies:
import { Router } from "express";

// Controllers:
import {} from "../controllers/funcionariosController.js"

const router = Router();

router.get("/")
router.post("/")
router.get("/:id")
router.put("/:id")
router.delete("/:id")

export default router;
