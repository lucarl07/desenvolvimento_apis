// Dependências:
import { Router } from "express";

// Controladores:

const router = Router();

router.post("/")
router.get("/:id_linha")
router.put("/:id_linha")
router.get("/")

export default router;