// Dependências:
import { Router } from "express";

// Controladores:

const router = Router();

router.post("/")
router.get("/:id_motorista")
router.delete("/:id_motorista")
router.get("/")

export default router;